# 基于 OpenGL 的实时渲染系统 - 需求梳理（当前实现状态）

## 1. 项目目标（一句话）

构建一个可交互的实时渲染编辑器，支持在场景中创建/导入对象并进行基础光照与材质渲染，同时提供节点化 Shader 编辑界面雏形。

## 2. 用户角色（当前版本）

### 2.1 场景编辑者（核心角色）

- 通过 UI 创建/导入/删除场景对象。
- 调整对象位姿（平移/旋转/缩放）并观察渲染效果。
- 切换渲染开关（天空盒、方向光、Blinn-Phong）。

### 2.2 渲染开发者（工程角色）

- 调整着色器、光照参数、渲染管线。
- 验证节点编辑器交互和后续 Shader 生成能力。

## 3. 核心业务对象

- `SceneObject`：统一场景实体，类型包含 `Cube` / `Light` / `Model` / `Image`。
- `BuildSystem`：场景对象容器与对象生命周期入口（创建、导入、删除、选中）。
- `Model`：通过 Assimp 导入的模型资源。
- `Texture`：图片/材质纹理资源（含普通 2D 纹理与天空盒立方体纹理）。

## 4. 核心业务模块与功能点

### 4.1 场景构建模块（Build）

#### 已实现

- 创建对象：`Cube`、`Light`、`Image`。
- 导入模型：菜单导入 + 文件拖拽导入（`obj/fbx/gltf`）。
- 删除对象：场景列表右键删除。
- 选中对象：鼠标点击拾取（射线与 AABB 相交）。

#### 当前约束

- 仅内存态场景，无项目保存/加载。
- `Model` 与 `Image` 在场景列表中命名未区分（列表名称显示不完整）。

### 4.2 场景编辑模块（Inspector + Gizmo）

#### 已实现

- 选中对象后支持 `TR/RT/SC` 三种操作模式。
- 支持 `Local/World` 变换空间切换。
- 支持直接输入变换数值（位置/旋转/缩放）。

#### 当前约束

- 缺少撤销/重做、复制粘贴、多选等编辑能力。

### 4.3 渲染表现模块（实时渲染）

#### 已实现

- 基础 PBR 前阶段能力：漫反射/镜面反射、法线贴图、Gamma 校正。
- 光照：方向光 + 最多 8 盏点光源（由场景中的 `Light` 对象驱动）。
- 阴影：方向光阴影贴图 + 点光源深度立方体阴影。
- 环境：天空盒开关、网格地面绘制。
- 透明物体：`Image` 按摄像机距离排序后绘制。
- 后处理：离屏渲染到 FBO 后再绘制到屏幕。

#### 当前约束

- 渲染参数与流程集中在主循环，模块边界弱。
- 点选包围盒对复杂模型精度有限（统一按立方体 AABB 处理）。

### 4.4 Shader 节点编辑模块（ShaderGraph）

#### 已实现

- 节点编辑窗口、节点创建、连线创建、删除交互。
- 预置示例节点图（Texture2D / Multiply / Output）。

#### 当前约束

- 尚未把节点图编译/导出为 GLSL。
- 节点删除逻辑存在已知 bug（源码中有 `todo`）。

## 5. 关键用户操作流程（当前）

1. 启动程序进入场景。
2. 打开 Build 面板创建 `Cube/Light/Image`，或导入模型。
3. 左键选择对象，使用 Gizmo 或 Inspector 调整变换。
4. 在渲染开关中控制天空盒、方向光、Blinn-Phong。
5. 通过 ShaderGraph 面板进行节点交互（当前为 UI 雏形验证）。

## 6. 当前痛点映射（与你反馈一致）

- 代码复用率低：大量逻辑集中在 `main.cpp` 主循环内，渲染阶段与业务操作耦合高。
- 功能未充分函数化：对象管理、渲染通道、状态切换存在重复代码片段。
- 可维护性风险：全局状态较多，后续扩展（材质系统、后处理链、保存系统）成本偏高。

## 7. 建议的需求优先级（下一步）

1. 抽离“场景服务层”：统一新增/删除/选中/查询对象接口，减少 UI 直接操作容器。
2. 抽离“渲染管线层”：按 Pass 拆分（阴影/主渲染/透明/后处理）。
3. 补齐“资源与项目层”：增加场景序列化（保存/加载）与资源缓存。
4. 推进 ShaderGraph：定义节点数据模型与 GLSL 代码生成规则。
